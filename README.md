# Fund Quant Lab

一个给交易新手使用的个人基金量化研究驾驶舱。第一版默认使用示例行情，不连接真实账户，不自动下单，目标是先建立研究、模拟和复盘习惯。

## 你能先做什么

- 查看每日市场状态：偏强、震荡或偏弱
- 看到 ETF 轮动策略给出的目标仓位
- 跑一个低频模拟账户，观察收益、回撤和换手
- 理解每个信号背后的简单原因
- 在学习卡片里查常见指标含义

## 快速开始

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install akshare
streamlit run app.py
```

打开终端显示的本地地址，一般是 `http://localhost:8501`。

## 项目结构

```text
.
├── app.py                         # 本地网页驾驶舱
├── fund_quant_lab/
│   ├── config.py                  # 默认基金池
│   ├── sample_data.py             # 示例行情生成
│   ├── strategy.py                # ETF 轮动和回测
│   ├── risk.py                    # 风险指标
│   ├── explain.py                 # 新手解释文案
│   └── data_sources.py            # 真实数据接口预留
├── tests/
│   └── test_core.py               # 基础行为测试
├── requirements.txt
└── requirements-real-data.txt     # 后续接真实数据时再安装
```

## 第一版策略逻辑

系统会对基金池里的 ETF 做三个简单判断：

- 近 60 个交易日是否表现强
- 近 20 个交易日波动是否过大
- 当前是否处在明显回撤中

当市场状态偏强时，策略倾向选择评分靠前的 ETF；当市场状态偏弱时，策略会提高国债、货币和黄金类资产的比例。默认每月调仓一次，适合先学习低频节奏。

## 真实数据

页面左侧默认选择「真实数据(AkShare)」。系统会拉取 ETF 日线行情，并缓存到 `data/cache/`，避免每次打开都重新下载。

当前真实数据设置：

- 数据源：AkShare 的东方财富 ETF 日线接口；如果东方财富不可用，自动切到新浪 ETF 日线备用源
- 价格口径：东方财富为后复权收盘价；新浪备用源为真实收盘价
- 刷新方式：点击左侧「刷新真实数据」
- 失败处理：如果网络或接口失败，会自动回退到示例数据，并显示失败原因

后续还可以接：

- AkShare：适合免费原型和日常拉取
- Tushare Pro：接口更系统，但部分基金数据有积分要求

真实数据适配入口在 `fund_quant_lab/data_sources.py`。

## 重要提醒

这个项目是研究和学习工具，不是投资建议。实盘前至少先用模拟账户跑一段时间，并确认你理解：

- 最大回撤可能比历史回测更大
- 轮动策略会有连续亏损期
- 基金交易有手续费、滑点、折溢价和流动性限制
- 场外基金还有申赎确认、限购和净值更新时间问题
